const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    colors: {
      primary: {
        light: '#0000ff',
        DEFAULT: '#0000ff',
        dark: '#0000ff',
      },
      secondary: colors.red,
      neutral: colors.gray,
      white: '#FFF',
    },
    extend: {},
  },
  variants: {},
  plugins: [],
};